services:
  - type: web
    name: pdf-editor-api
    env: node
    region: oregon
    buildCommand: cd server && npm install
    startCommand: cd server && npx tsx src/index.ts
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: PORT
        value: 10000
